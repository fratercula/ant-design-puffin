import {
  Divider,
  Progress,
  Alert,
  Icon,
  Tag,
  // Tooltip,
  Button,
} from 'antd'
import component from './component'

export default {
  ...component,
  Divider,
  Progress,
  Alert,
  Icon,
  Tag,
  // Tooltip,
  Button,
}
